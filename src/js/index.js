const filterElement = document.querySelector('header input')

const cards = document.querySelectorAll('.content li')

filterElement.addEventListener('input', filterCards)

function filterCards() {
}